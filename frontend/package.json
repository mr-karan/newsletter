{
  "dependencies": {
    "prettier": "^1.19.0",
    "tailwind": "^4.0.0",
    "watch": "^1.0.2"
  },
  "scripts": {
    "build:css": "tailwind build css/*.css -o static/output.css",
    "watch": "watch 'yarn build:css' ./css",
    "start:dev": "python3 -m http.server --bind 0.0.0.0 3000"
  },
  "devDependencies": {
    "tailwindcss": "^1.1.3"
  }
}
