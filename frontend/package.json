{
  "dependencies": {
    "live-server": "^1.2.1",
    "prettier": "^1.19.0",
    "tailwind": "^4.0.0",
    "watch": "^1.0.2",
    "lodash": "^4.17.13"
  },
  "scripts": {
    "build": "tailwind build css/*.css -o static/output.css && cp index.html static/index.html",
    "watch": "watch 'yarn build' ./css",
    "start:dev": "live-server . --port=3000"
  },
  "devDependencies": {
    "tailwindcss": "^1.1.3"
  }
}